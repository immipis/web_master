    // // Document object : 객체(속성 메소드)
    // let ulDom = document.querySelector(`ul`);
    // console.log(ulDom.children[0].innerHTML);
    // // dom 객체에는 메소드 (객체를 생성하는 메소드 : createElement())
    // let liTag = document.createElement(`li`);//<li></li>요소를 생성.
    // liTag.innerHTML = '수박'
    // ulDom.appendChild(liTag)
    // console.log(ulDom)

    // let pTag = document.createElement(`p`)
    // pTag.innerHTML = '<b>Welcome</b>'
    // document.querySelector(`#show`).appendChild(pTag)

    // let bTag = document.createElement(`button`)
    // bTag.innerText = '<b>삭제</b>'
    // bTag.addEventListener('click', (e)=>{
    //   console.log(e.target.previousElementSibling.remove())
    //   console.log(document.querySelector(`ul`).children[0].remove())
    //   console.log(document.querySelector(`ul>li:nth-of-type(1)`).remove())
    // })
    // document.querySelector(`#show`).appendChild(bTag)
